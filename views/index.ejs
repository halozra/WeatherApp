<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WeatherApp</title>
    <link rel="stylesheet" href="styles/style.css">
</head>
<body>
    <div class="weather">
        <form action="/" method="post">
            <input type="text" name="inputUser" id="inputUser" placeholder="Input Your City..." required>
            <button type="submit"><img src="img/icons8-search.svg" alt="search"></button>
        </form>
        <div class="weatherP">
        <% if(data){ %>
            <h1><%= data.name %></h1>
            <div class="weather_icon">
                <img src="https://openweathermap.org/img/wn/<%= data.weather[0].icon %>@2x.png" alt="" srcset="">
                <p><%= data.main.temp %>°C </p>
            </div>
            <div class="weather-desc">
                <p>Humidity: <%= data.main.humidity %></p>
                <p>wind speed: <%= data.wind.speed%></p>
            </div>
        <% }else{ %>
            <p>City is not found</p>
        <% } %>
        </div>
    </div>

</body>
</html>